# vruneel-wedding
Vrunda & Neel Wedding Memories website built with React + Vite, Tailwind CSS, Framer Motion, and Lucide icons.

## Local Development
- npm.cmd install
- npm.cmd run dev

## Build
- npm.cmd run build

## Deploy
GitHub Pages deploys automatically from `.github/workflows/deploy.yml` on push to `main`.
